# Ralph Progress Log
Started: Tue Jun 30 14:37:57 EDT 2026
---

## Codebase Patterns
- New unit test files under tests/unit/ MUST declare `pytestmark = pytest.mark.unit`
  (pyproject addopts = `-m unit`); without it the file is silently deselected (0 run).
- pytest defaults to unit only. Mark integration/e2e with `pytest.mark.integration`/`e2e`
  and skip-guard on the live resource (~/.sema/poc.duckdb, Neo4j, Databricks creds).
- mypy is strict (pyproject [tool.mypy] strict=true). Type hints everywhere; run
  `uv run mypy src/sema/` before committing.
- R29 boundary: src/sema/engine/ (and the generic spine) must contain NO OMOP/OncoTree
  literals (OncoTree/oncotree/cBioPortal/cbio, "Maps to", standard_concept,
  condition_occurrence, concept_id). Domain-specific vocab knowledge belongs OUTSIDE
  engine core as data (e.g. src/sema/models/vocab_knowledge.py); OMOP specifics live in
  authored manifests (targets/manifests/*.yaml) and policy modules. The migration check
  lives in tests/unit/test_engine_coupling.py (US-001 will formalize the full guard).
- The targets/ framework (PR #109, ea2aed7) is MERGED to main and R29-clean. Build on it
  (load_target, ManifestTargetAdapter, materializer -> Neo4j/InMemory, `sema target load`);
  do NOT reimplement loaders/materializers. Do NOT grow/refactor loader.py or
  materializer_utils.py.
- Coverage gate >= 85% (`uv run pytest --cov=sema --cov-report=term-missing`); repo
  currently ~93%.
- ONCOTREE_CODE source-of-truth: `cbioportal_<study>.sample.ONCOTREE_CODE` only (2
  studies in ~/.sema/poc.duckdb). Discover studies via information_schema.columns
  (column_name='ONCOTREE_CODE' AND table_name='sample'); 64 distinct codes.
- GOLD-SET-CONSUMING code must NOT live under an R29 core path (compile/,
  resolve/engine.py, resolve/assembler.py): GoldRow.oncotree_code attribute access
  contains the "oncotree" denylist substring and trips the guard. Put it in eval/
  (allowlisted) or a non-scanned *_utils.py. Staging QA (US-011) lives in
  src/sema/eval/staging_qa.py(+_utils) for exactly this reason.
- §1.5(f) metric math is FROZEN in src/sema/eval/mapping_goldset_utils.py (US-002):
  derive_zone/classify_cell/ConfusionMatrix. US-012 MUST reuse it (no re-derived
  math). Recall denominator includes Zone-2 recall_miss (reduces to the doc's
  TP/(TP+WRONG+FN) when no Zone-2 predictions exist).
- Gold-set artifact tests/data/gold/oncotree_condition_slice0.jsonl: gold_concept_id
  is an EXTERNAL ORACLE (human/crosswalk), never Sema's resolver. Codes awaiting the
  human gate use gold_label=UNLABELLED (excluded from scoring, surfaced separately).
- SQL query layers use SQLGlot ASTs (exp.select/.from_/.where/.join), rendered per
  dialect via .sql(dialect="duckdb"|"databricks"); bind values as exp.Placeholder()
  (`?`), never embed them. OMOP physical table/column names are config
  (resolve/policies/omop.py:OMOP_VOCAB_SCHEMA), not hardcoded in the query module.
  VocabStore._fetch handles both DuckDB (execute->result.fetchall) and DBAPI cursors
  (execute->None, conn.fetchall). poc.duckdb concept tables are ALL-VARCHAR.
- §1.5(a) value-mapping store schema is FROZEN in
  src/sema/resolve/value_mapping_store_utils.py (FROZEN_COLUMNS + GRAIN_KEY constants);
  the ValueMapping dataclass + SQL builders live there too. Downstream stories
  (US-006 writes; US-010/US-012 read) MUST import these, never re-list columns. The
  drift test (tests/integration/test_value_mapping_store_schema.py) freezes the contract.
- DuckDB upsert pattern: declare PRIMARY KEY on the grain columns, then
  `INSERT ... ON CONFLICT (grain) DO UPDATE SET col = excluded.col` (one row per
  execute() call — a single statement can't update the same conflict row twice).
- Hermetic DuckDB unit tests use a real temp file (tmp_path / "x.duckdb"), not a fake
  conn — established by tests/unit/test_duckdb_staging.py. In-process DuckDB is local
  (no network), so it stays within the hermetic-unit-suite rule.
- R29 SCANNED CORE PATH: src/sema/resolve/engine.py (and resolve/assembler.py, compile/)
  are grepped by the guard; the literal `concept_id` IS on the denylist. So engine.py /
  assembler.py / compile/*.py must NOT contain the substrings concept_id / standard_concept
  / "Maps to" / condition_occurrence / OncoTree casings. Keep ALL concept-id-touching logic
  (ValueMapping/CodeResolution construction, ConceptRow.id casts) in a NON-scanned sibling
  *_utils.py (e.g. resolve/engine_utils.py); the scanned orchestrator only delegates. The
  other resolve spine files (candidates/standardize/domain_gate/disambiguate/*_utils) are
  NOT scanned but stay generic anyway. Run `uv run python scripts/check_engine_coupling.py`.
- §4 resolver wiring (US-006): VocabularyResolver(store, policy).resolve(code) -> CodeResolution.
  Stages: candidates.generate_candidates (store.concept_by_code via policy.candidate_lookup,
  R9 SOURCE vocab) -> standardize.standardize (store.maps_to_targets w/ policy.maps_to_relationship
  + standard_flag + only_valid) -> domain_gate.apply_domain_gate (policy.in_target_domain).
  1 survivor=auto_accepted RESOLVED conf 1.0 (Zone-1); 0=NO_MAP conf 1.0 (Zone-3); >1 tie=
  review_pending RESOLVED, deterministic min-id placeholder, conf 0.5 (Zone-2). NO LLM on the
  code-bearing hot path (disambiguate default declines ties -> None). resolve_and_store(codes,
  ValueMappingStore, ResolveContext) is the SOLE store writer. ConceptRow.id is str -> cast
  int() at the value-mapping boundary (poc.duckdb concept tables are all-VARCHAR).
- Authored targets live as YAML manifests under src/sema/targets/manifests/ (R29-allowlisted
  by extension — the guard scans *.py only). Load via the MERGED framework: load_target(
  ManifestTargetAdapter(path), writer=InMemoryGraphWriter()|Neo4jGraphWriter(driver)); never
  reimplement loaders/materializers. The framework ALREADY emits VocabularyBindingOp (with
  resolver_policy_ref/domain/require_standard) + HAS_VOCABULARY_BINDING edges, EntityOp/
  PropertyOp/TargetObligationOp, and HAS_PROPERTY/HAS_OBLIGATION relationships. Manifest FKs
  must reference an entity present in the same manifest (normalizer rejects dangling refs);
  unknown vocab refs are rejected too — declare every vocabulary in `vocabularies:`.
- §1.5(d)/(e) plan assembly (US-008): FieldMap now carries `status: Status`
  (default candidate). `FieldMap.covers_required_field()` is the §1.5(e) gate — a
  rejected status, NO_MAP pattern, or CONSTANT(NULL) literal NEVER covers a required
  field. `MappingPlan.covered_required_fields()` filters on it; `derive_verdict()`
  computes `any_review_pending` from field-map statuses (any review_pending → verdict
  awaiting_review, which short-circuits ahead of obligation/fk checks in
  derive_plan_verdict). Coverage matching is EXACT string equality between
  obligation.required_fields and FieldMap.target_field_ref, so both must use the SAME
  dotted RefStr (target_field_ref needs ≥1 dot; bare names like "condition_concept_id"
  are NOT valid RefStr). planner_loader field_map serialization now includes `status`
  (read uses .get(default=candidate) for back-compat). The concrete assembler is
  src/sema/resolve/assembler.py:Slice0PlanAssembler (R29-SCANNED → generic, names no
  domain literal): groups assertions by target_property_ref, select_winner per group
  (planner mapping_plan.py:112, NOT winner_selection.py), projects winner→FieldMap
  carrying winner.status, emits RISK_ASSEMBLER_CONFLICT_RESOLVED (Severity.info, never
  blocks) on >1 live assertion. The Slice-0 staging obligation
  (make_slice0_staging_obligation) lives in the ALLOWLISTED policies/omop.py (it names
  condition_concept_id); required={condition_concept_id, resolver_policy_ref,
  vocab_release}, no person_id/dates. KNOWN pre-existing integration failure (bug-347):
  test_graph_loader_integration::test_different_sources_coexist fails on HEAD too — NOT
  a US-008 regression (graph/loader MERGE-key over-collapse).
- Neo4j target-graph schema gotchas: :Entity has `qualified_name` (NO `name`);
  :TargetObligation stores the obligation as a single `payload_json` JSON string (no
  per-field properties). Edge name is HAS_VOCABULARY_BINDING (not HAS_VOCAB_BINDING).
  ManifestVocabularyBinding accepts resolver_policy_ref, which resolve_policy() (US-004)
  dispatches to a concrete ResolverPolicy.
- US-009 graph producer (src/sema/resolve/producer.py + producer_utils.py): VOCAB_LOOKUP
  is a COLUMN-level mapping — ONE MappingAssertion + ONE FieldMap for the whole source
  column, NOT one per code. Per-code decisions stay in the value-mapping store (US-006
  sole writer); the producer only READS via a DecisionSource Protocol (read_all()), never
  writes. Reuse engine_utils.build_vocab_lookup_assertion with a synthetic column-level
  CodeResolution (source_code=context.source_field_ref, concept_id=None, survivors=()) —
  the assertion builder only consumes resolution_status/source_code/confidence/status.
  Status aggregates: review_pending if any decision is review_pending else auto_accepted;
  confidence=min. Edges reuse planner_loader cypher builders: cypher_create_field_map_
  maps_to/derived_from return (stmt, params) tuples → session.run(stmt, **params). MAPS_TO
  requires ModelRole.TARGET / DERIVED_FROM ModelRole.SOURCE (require_property_role_for_
  relationship enforces it); MAPS_TO ALWAYS originates at the :FieldMap. Target :Property
  carries n.id (=`{model_id}|{ver}|{hash}|{entity}.{name}`) + n.name + n.model_role; query
  by name+model_role to fetch the id for MappingNodes. producer.py is NOT R29-scanned but
  stays generic (refs/ids passed as data).
- US-010 transform compiler (src/sema/compile/ — R29-SCANNED): `concept_id` is a
  DENYLIST substring, so compile/*.py must NEVER access `value_mapping.concept_id`.
  The compiler consumes a generic `StagingDecision(normalized_source_value, target_value,
  resolution_status, no_map_reason, status)` — the ValueMapping->StagingDecision projection
  (which DOES read .concept_id) lives in the unscanned `resolve/engine_utils.py`
  (`staging_decision_from_value_mapping`). Field name `target_concept_column` is SAFE
  (no `concept_id` substring); showcase column names (`source_oncotree_code`,
  `condition_concept_id`) live ONLY in the allowlisted `policies/omop.py:OMOP_STAGING_COLUMNS`
  (a `StagingColumns` instance). `StagingColumns` required showcase fields have NO defaults
  (so the literals can't sneak into compile/ as defaults); generic columns have neutral
  defaults. The §1.5(b) staging SELECT is built ONCE as a SQLGlot AST (source LEFT JOIN
  `(VALUES (...)) AS m(code,target_value,...)` ON src.value=m.code) and rendered per dialect
  via `compiled.sql("duckdb"|"databricks")` — NO per-dialect string templates. Idempotency =
  `CREATE OR REPLACE TEMP TABLE` build, then BEGIN/DELETE-scope/INSERT/COMMIT scoped on
  (source_schema, source_table) so sibling studies in the same schema survive a swap. NO_MAP
  rows carry target_value=NULL inline -> `<target_concept_column>` IS NULL. Dispatch is on
  (MappingPattern, TargetArtifactKind); Slice 0 registers only (VOCAB_LOOKUP, TABLE_ROW).
  NOTE: sqlglot `exp.alias_` returns the `exp.Expr` union (not `exp.Expression`) — type
  projection lists as `list[exp.Expr]` or mypy strict fails. Databricks renders VALUES
  without wrapping parens but still parses (US-013 owns live execution).
- US-012 mapping eval report (src/sema/eval/mapping_report.py + mapping_report_utils.py,
  eval/ R29-allowlisted so it may read the store's `concept_id` + reconcile gold): does
  NOT re-derive metric math — build_mapping_report() calls eval.mapping_goldset.score()
  (the frozen §1.5(f) ConfusionMatrix). decision_from_value_mapping() bridges the TWO
  ResolutionStatus enums (resolve's + eval's, identical values) by `.value`. Acceptance
  is GATED, never self-certified: AcceptanceVerdict {ACCEPTED, PROVISIONAL_NOT_ACCEPTED,
  RUNNING_NOT_ACCEPTED}. evaluate_acceptance(matrix, coverage): coverage<1.0 →
  PROVISIONAL (US-002 human-label gate open, surfaces unlabelled_codes); full coverage +
  precision>=0.95 + auto_resolution>=0.70 → ACCEPTED; else RUNNING. Thresholds
  MIN_MAPPED_PRECISION=0.95 / MIN_AUTO_RESOLUTION_RATE=0.70. GOTCHA: auto_resolution_rate
  = zone1/scored and `scored` INCLUDES correctly-predicted NO_MAP gold (tn) — so a gold
  set with many NO_MAP codes drags auto_resolution down even at perfect precision (it is
  NOT tp/RESOLVED-only). no_map_accuracy is reported but is NOT a gating threshold.
  STRUCTURAL_PRECISION_CAVEAT is always emitted (Slice-0 ~100% precision is a property of
  the deterministic exact-code walk, not the product). report.as_dict() = JSON (verdict +
  coverage + thresholds + 3-granularity metrics + caveat); human_summary() = text.

---

## 2026-06-30 - US-000 (R29 baseline migration)
- Neutralized the 4 known OMOP/OncoTree leaks in src/sema/engine/ so the R29 denylist
  returns EMPTY against engine core, clearing the way for the US-001 guard.
- TDD: wrote tests/unit/test_engine_coupling.py first (case-explicit denylist scan of
  engine core + a scanner sanity test on a seeded literal); confirmed it failed on the
  4 real leaks (5 hits — "cBioPortal" matches both cBioPortal and cBio), then fixed.
- Per-file neutralization (3 generalized in place, 1 relocated):
  - engine/domain_prompts.py:83 — GENERALIZED: "Cancer classification systems (OncoTree,
    ICD-O)" -> "Cancer classification/morphology systems (ICD-O and similar)". The hint
    already instructs naming families not specific ontologies; prompt-text only.
  - engine/few_shot_healthcare.py:3 — GENERALIZED: docstring "sourced from cBioPortal
    analyst questions" -> "sourced from oncology warehouse analyst questions".
  - engine/few_shot_healthcare_stage_a.py:31 — GENERALIZED: few-shot column
    `oncotree_code` -> `cancer_type_code` (still a realistic cancer-type code column).
  - engine/vocabulary_utils.py:182 — RELOCATED: moved the HIERARCHICAL_VOCABULARIES
    frozenset (the only set carrying "oncotree") to a new allowlisted home,
    src/sema/models/vocab_knowledge.py, and imported it back. Behavior preserved exactly
    (still re-exported as vocabulary_utils.HIERARCHICAL_VOCABULARIES). NON_HIERARCHICAL_TYPES
    stayed (no leak).
- Files changed: NEW src/sema/models/vocab_knowledge.py, NEW
  tests/unit/test_engine_coupling.py; EDIT engine/domain_prompts.py,
  engine/few_shot_healthcare.py, engine/few_shot_healthcare_stage_a.py,
  engine/vocabulary_utils.py.
- Quality: `uv run pytest` 1911 passed / 1 skipped; `uv run mypy src/sema/` clean;
  coverage 93% (vocab_knowledge.py + test_engine_coupling.py both 100%).
- NOTE (per AC): PR #109 (ea2aed7) is MERGED to main with the R29-clean targets/
  framework; US-004/US-007 BUILD ON it — no recovery, no reimplementation.
- **Learnings for future iterations:**
  - The denylist scanner uses case-explicit substrings; "cBioPortal" yields two hits
    (cBioPortal + cBio) — expect N>4 violation rows for the 4 source leaks. US-001's
    formalized guard should keep the casing explicit (the AC forbids casing bypass).
  - No test asserted "oncotree" membership in HIERARCHICAL_VOCABULARIES, so relocation
    was safe; the only consumer is engine/vocabulary_utils.should_infer_hierarchy.
  - .wolf/anatomy.md and .wolf/memory.md are auto-maintained by OpenWolf hooks on
    file create/edit — no manual anatomy/memory updates needed this iteration.
  - engine/vocabulary_utils.py uses stdlib `logging`, not loguru (pre-existing); left
    as-is to keep the change focused.
---

## 2026-06-30 - US-001 (Executable R29 OMOP-coupling guard)
- Formalized the R29 guard from US-000's denylist into a reusable, CI-wired check.
- NEW scripts/check_engine_coupling.py: ONE frozen policy constant R29_POLICY
  (EngineCouplingPolicy dataclass) owning denylist + core_paths + allowlist;
  scan(root, policy) -> list[Violation]; find_violations() over the live tree;
  main() CLI (exit 1 on leak). Core paths grepped: src/sema/engine,
  resolve/engine.py, resolve/assembler.py, compile/, targets/. Allowlist:
  resolve/policy.py, resolve/policies/, targets/adapters/, eval/, tests/.
- REWROTE tests/unit/test_engine_coupling.py (was US-000's inline scan): imports
  from the script now; adds fixture-positive (leaky core file -> non-empty),
  clean-passes, allowlisted-file-exempt, missing-core-path-skipped,
  case-explicit denylist, and the live-tree green assertion.
- Quality: uv run pytest 1915 passed / 1 skipped; mypy clean (src/sema/ AND the
  script); coverage 93%.
- **Learnings for future iterations:**
  - scripts/ is a real package (has __init__.py) and IS importable from tests
    (e.g. `from scripts.check_engine_coupling import ...`); existing precedent:
    tests/unit/test_slice_contamination.py, tests/scripts/test_migrate_cbioportal.py.
  - scripts/ is OUTSIDE --cov=sema scope, so guard/CLI code adds no coverage
    pressure -- but the unit test still exercises it for correctness.
  - The guard SKIPS non-existent core paths on purpose: resolve/engine.py,
    resolve/assembler.py, compile/ don't exist yet (US-006/US-008/US-010 create
    them). Once they land they're auto-covered -- no guard edit needed. Just keep
    new core/spine modules under a configured core_path (or add the path here).
  - When you add the US-004 policy modules (resolve/policy.py, resolve/policies/)
    the allowlist ALREADY exempts them; OMOP literals there won't trip the guard.
  - Denylist matching is plain substring per line over *.py only; manifests
    (*.yaml) are never scanned, so OMOP literals in authored manifests are fine.
  - DCO: commits on this branch carry a `Signed-off-by:` trailer (git commit -s).
---

## 2026-06-30 - US-002 (Slice-0 gold set + mapping eval harness)
- Shipped the §1.5(f) confusion-matrix eval harness + the gold-set scaffold.
  NO resolver (per AC) — only the metric module and the artifact.
- NEW src/sema/eval/mapping_goldset_utils.py: frozen home of the metric math —
  GoldLabel{RESOLVED,NO_MAP,UNLABELLED}, ResolutionStatus, Zone (derived §1.5(c)),
  GoldRow/Decision dataclasses, derive_zone(), classify_cell() (tp/wrong/fn/
  recall_miss/tn/fp_map/na), ConfusionMatrix with mapped_precision/mapped_recall/
  auto_resolution_rate/no_map_accuracy (None on empty denom), frequency_bucket().
- NEW src/sema/eval/mapping_goldset.py: load_gold_set (JSONL), GoldSet
  (labelled/unlabelled accounting = the human gate), score() ->
  GoldSetReport at distinct-code, row-weighted, and per-frequency-bucket
  granularity; distinct_oncotree_sql()/discover_oncotree_schemas()/
  enumerate_distinct_codes() for live enumeration + scaffolding.
- NEW tests/data/gold/oncotree_condition_slice0.jsonl: 64 distinct ONCOTREE_CODE
  rows (gbm_tcga_pan_can_atlas_2018 + msk_chord_2024 `sample` tables, 25,632 src
  rows total). gold_label=UNLABELLED, gold_concept_id=null, row_count from the
  documented query, notes per code. HUMAN-LABEL GATE: 0/64 labelled — Ralph did
  NOT invent labels (auto-labelling would make US-012 circular). The unlabelled
  remainder is surfaced by GoldSet.unlabelled_codes(); completing 100%
  distinct-code coverage is the human gate US-012 acceptance depends on.
- NEW tests/unit/test_mapping_goldset.py (29 hermetic tests): the synthetic
  gold+decision set computes to the exact §1.5 values (precision 0.6, recall 0.6,
  auto_res 5/7, no_map_acc 0.5 reported SEPARATELY); NO_MAP-for-real-concept = FN
  (never dropped); NO_MAP-for-NO_MAP = TN; Zone-2 = recall_miss.
- NEW tests/integration/test_mapping_goldset_coverage.py (skip-guarded on
  ~/.sema/poc.duckdb): asserts the gold set covers every observed code (no drift)
  and row_counts match live data. Ran live: 2 passed (64/64 covered).
- Quality: uv run pytest 1944 passed / 1 skipped; mypy clean (178 files);
  coverage 93% repo, 98% on the two new modules.
- **Learnings for future iterations:**
  - ONCOTREE_CODE lives ONLY in `cbioportal_<study>.sample.ONCOTREE_CODE` (2
    studies in poc.duckdb: gbm_tcga_pan_can_atlas_2018, msk_chord_2024); 64
    distinct non-empty codes, 25,632 rows. There is NO `cbioportal_*` named table
    other than per-study schemas — discover via information_schema.columns on
    column_name='ONCOTREE_CODE' AND table_name='sample'.
  - eval/ is ALLOWLISTED by the R29 guard (US-001), so OncoTree/cbioportal
    literals are FINE in eval modules, tests, and the gold artifact. The guard
    only scans *.py under engine core paths; JSONL/data files are never scanned.
  - §1.5(f) recall is subtly under-specified: the doc formula TP/(TP+WRONG+FN)
    omits Zone-2 ("recall miss"). I implemented the full gold-RESOLVED
    denominator TP/(TP+WRONG+FN+recall_miss) so a Zone-2 prediction lowers recall
    per the prose; when recall_miss=0 (the common Slice-0 case) it reduces
    EXACTLY to the documented formula. US-012 must reuse this module's math, not
    re-derive it.
  - FP_map arises ONLY for gold=NO_MAP (a Zone-1 prediction); a Zone-1 wrong
    concept for gold=RESOLVED is WRONG, not FP_map. no_map_accuracy = TN/(TN+
    FP_map) and is reported SEPARATELY — never folded into the mapped_* metrics.
  - GoldLabel.UNLABELLED is a third sentinel (beyond the §1.5 RESOLVED|NO_MAP)
    representing "scaffolded, awaiting the human oracle". score() excludes these
    and lists them in GoldSetReport.unscored_unlabelled so they can never be
    silently scored as labelled.
---

## 2026-06-30 - US-004 (Per-vocabulary resolver policy object)
- Shipped the vocabulary-agnostic ResolverPolicy so OMOP specifics sit behind a
  boundary and source-vs-target conflation (R9) is structurally impossible.
- NEW src/sema/resolve/policy.py: ResolverPolicy frozen dataclass exposing
  SOURCE-side data (source_vocabulary, maps_to_relationship, standard_flag) and
  reading the TARGET obligation (target_domain, require_standard,
  allow_zero_default) FROM the bound VocabularyBindingDecl via @property — the
  fields are NOT duplicated. Candidate dataclass (standard_value/is_invalid/
  domain) + the validity predicate accepts() = valid AND (standard if required)
  AND in target domain; plus is_standard/is_valid/in_target_domain and
  candidate_lookup(code) -> (source_vocabulary, code) for R9.
- NEW src/sema/resolve/policies/omop.py: the ONLY module naming OncoTree /
  "Maps to" / "S"; make_omop_oncotree_condition_policy(binding) factory +
  OMOP_ONCOTREE_CONDITION_REF constant.
- NEW src/sema/resolve/policies/__init__.py: resolve_policy(binding) dispatches
  binding.resolver_policy_ref through a module-level _FACTORIES constant;
  UnknownResolverPolicyError on missing/unknown ref.
- Consumed the MERGED binding model src/sema/models/target/vocab_binding.py
  (VocabularyBindingDecl: domain/require_standard/allow_zero_default/
  resolver_policy_ref/vocabulary) — fields read, never re-declared.
- TDD: tests/unit/test_resolver_policy.py (13 tests) written first, failed with
  ModuleNotFoundError: No module named 'sema.resolve', then implemented.
- Quality: uv run pytest 1957 passed / 1 skipped; mypy clean (182 files); R29
  guard clean; coverage 93% repo, 100% on all four new resolve modules.
- **Learnings for future iterations:**
  - R9 boundary made concrete: ResolverPolicy.source_vocabulary is the SOURCE
    (OncoTree); binding.vocabulary.name is the TARGET (SNOMED). US-006's
    candidates.py must match the source code in policy.candidate_lookup()'s
    SOURCE vocab, NOT the binding's target vocab.
  - "Read, don't duplicate" pattern: target_domain/require_standard/
    allow_zero_default are @property reads off self.binding, so changing the
    obligation needs no policy change. US-007's manifest sets these on the
    VocabularyBindingDecl; the policy picks them up for free.
  - resolve/policy.py and resolve/policies/ are BOTH allowlisted by the R29
    guard, but only policies/omop.py actually names OMOP literals — policy.py and
    policies/__init__.py are kept generic by design (defense in depth).
  - VocabularyBindingDecl is frozen + extra='forbid'; build test bindings with
    all fields. TargetEntityRef.qualified_name needs >=1 dot (regex), e.g.
    "omop.condition_occurrence".
  - resolve_policy expects a registered ref; US-007's manifest must set
    resolver_policy_ref="omop.oncotree_to_snomed_condition" (OMOP_ONCOTREE_
    CONDITION_REF) for the binding to resolve.
---

## 2026-06-30 - US-003 (Vocabulary store query layer over vocabulary_omop)
- Shipped the thin SQLGlot-based query layer over the OMOP concept tables. One
  query authored once, rendered per dialect (DuckDB dev / Databricks prod), so
  the 10M-concept vocabulary stays in the warehouse, never Neo4j.
- NEW src/sema/resolve/vocab_store_utils.py: generic VocabStoreSchema (role-based
  field names — id_col/code_col/standard_col/...), ConceptRow (generic shape:
  id/name/domain/vocabulary/standard/code/invalid_reason), and SQLGlot AST
  builders concept_by_code_query / maps_to_targets_query (JOIN, optional
  standard+validity filters) / concept_domain_query. All return exp.Select — no
  hand-concatenated SQL. Placeholders render as `?` (bind params), so the
  relationship name ('Maps to') and standard flag ('S') are NEVER embedded in SQL.
- NEW src/sema/resolve/vocab_store.py: VocabStore(connection, schema, namespace,
  dialect) with concept_by_code / maps_to_targets / concept_domain. _fetch()
  handles BOTH DuckDB (execute returns a result with fetchall) and DBAPI cursors
  (execute returns None -> conn.fetchall()), so the same class serves DuckDB now
  and Databricks in US-013. open_duckdb_vocab_store() factory (read-only, FileNotFound
  guard); VocabStoreBackend enum + namespace_for_backend() (duckdb=vocabulary_omop,
  databricks=workspace.vocabulary_omop).
- EDIT src/sema/resolve/policies/omop.py (R29-allowlisted): added OMOP_VOCAB_SCHEMA
  — the ONLY place the OMOP column literals (concept_id, standard_concept, domain_id,
  concept_relationship, ...) live. VocabStore reads them as config so the query
  layer stays vocabulary-agnostic.
- TDD: tests/unit/test_vocab_store.py (16 hermetic tests, fake conn records SQL+params)
  written first, failed ModuleNotFoundError, then implemented. Asserts: namespaced
  SELECTs, 2-placeholder code lookup, JOIN with relationship bound (NOT literal),
  standard+validity filter pushed to SQL, both-dialect rendering, DBAPI-cursor path,
  builders return exp.Select. NEW tests/integration/test_vocab_store_live.py +
  tests/integration/_omop_binding.py (skip-guarded on ~/.sema/poc.duckdb): LUAD
  (OncoTree) -> concept_id 777926 domain Condition; maps_to_targets w/ policy's
  'Maps to'/'S'/only_valid -> standard SNOMED Condition concept 45768916; latency
  RECORDED at ~0.5s (bound asserted <30s) on the 54.9M concept_relationship table.
- Quality: uv run pytest 1976 passed / 1 skipped (+ 2 live integration passed);
  mypy clean (184 files); R29 guard clean; coverage 93% repo, 96% vocab_store.py /
  100% vocab_store_utils.py.
- **Learnings for future iterations:**
  - resolve/vocab_store.py and vocab_store_utils.py are NOT in the R29 guard's
    core_paths (only resolve/engine.py + resolve/assembler.py are), so they aren't
    scanned. But the US-003 AC still forbids hardcoding 'Maps to'/'S' — those are
    bound as `?` params from the policy. The OMOP COLUMN names live in
    policies/omop.py:OMOP_VOCAB_SCHEMA (allowlisted), passed in as config.
  - OMOP concept tables in poc.duckdb are ALL-VARCHAR (concept_id is a string
    '777926', not int). ConceptRow.id is str. US-005/US-006 must treat concept_id
    as a string from this store (cast at the store/compile boundary if an int is
    needed for the §1.5(a) store column).
  - OncoTree vocabulary_id is exactly 'OncoTree' (also 'OncoKB' present). 'Maps to'
    relationship_id + standard_concept='S' + invalid_reason IS NULL is the standard
    walk; LUAD->45768916 (SNOMED, Condition) verified live.
  - SQLGlot: exp.select(...).from_(...).where(a).where(b) ANDs the wheres;
    exp.Placeholder() renders as `?`; exp.to_table("a.b.c") parses dotted
    catalog.db.table so one namespace string works for both DuckDB (vocabulary_omop)
    and Databricks (workspace.vocabulary_omop). exp.column(name, table="alias")
    qualifies; .is_(exp.null()) -> `IS NULL`.
  - US-006 (resolver) wires policy -> store: candidates.py calls
    store.concept_by_code(policy.source_vocabulary, code); standardize.py calls
    store.maps_to_targets(cid, relationship_id=policy.maps_to_relationship,
    standard_flag=policy.standard_flag if policy.require_standard else None,
    only_valid=True); domain_gate.py uses ConceptRow.domain vs policy.target_domain.
---

## 2026-06-30 - US-005 (Resolved value-mapping store)
- Shipped the §1.5(a) DuckDB-backed value-mapping store: the location-independent
  decision cache (source_code -> concept_id | NO_MAP) the compiler reads from.
  NO resolver (US-006 writes it); this story is the store + frozen schema only.
- NEW src/sema/resolve/value_mapping_store_utils.py: FROZEN_COLUMNS (15, in table
  order) + GRAIN_KEY (5) module constants; ResolutionStatus enum (RESOLVED|NO_MAP,
  mirrors eval's — kept local to avoid editing the frozen US-002 module); ValueMapping
  frozen dataclass with __post_init__ invariants (concept_id NULL iff NO_MAP;
  no_map_reason non-null iff NO_MAP; RESOLVED forbids reason; confidence in [0,1]);
  to_params/from_row and SQL builders (create_table/upsert/select_all/select_by_grain).
- NEW src/sema/resolve/value_mapping_store.py: ValueMappingStore(conn, schema, table)
  + open_duckdb_value_mapping_store(db_path) factory. PRIMARY KEY = the §1.5(a) grain;
  upsert() uses ON CONFLICT DO UPDATE (re-write of same grain updates, never dups).
  read_all/get(by grain)/column_names/count; context manager. Module docstring records:
  DuckDB is the canonical home, NO Databricks seed sync (compiler inlines, US-010),
  US-006 is the SOLE writer.
- NEW tests/unit/test_value_mapping_store.py (20 hermetic tests, temp DuckDB):
  frozen-columns-match-spec, grain-key-match-spec, created-table-cols==FROZEN_COLUMNS,
  location-independent (no source_schema/table), write+read-back, get-by-grain,
  upsert-same-grain (count stays 1, value updated), different-vocab_release => 2 rows,
  NO_MAP persists NULL concept + reason, all 5 Status values round-trip, and the four
  ValueMapping validation guards.
- NEW tests/integration/test_value_mapping_store_schema.py (drift guard + real-code
  round-trip): live table cols == FROZEN_COLUMNS exactly; uses a temp DuckDB so it
  always runs under -m integration (not gated on a warehouse).
- Quality: uv run pytest 1993 passed / 1 skipped (+2 integration passed); mypy clean
  (186 files); R29 guard green; coverage 93% repo, 94% store / 98% utils.
- **Learnings for future iterations:**
  - concept_id is BIGINT / int|None in the store (aligns with the gold set's int and
    eval's Decision), DESPITE poc.duckdb's all-VARCHAR concept tables (US-003). US-006
    must CAST the VocabStore's str concept_id to int before writing a ValueMapping.
  - DuckDB ON CONFLICT needs a PRIMARY KEY/UNIQUE on the conflict columns; the grain is
    the PK. Upsert one ValueMapping per execute() — a single INSERT...VALUES with the
    same grain twice raises "can not update the same row twice".
  - ResolutionStatus now exists in TWO places (resolve + eval) with identical values.
    Kept separate to avoid touching the frozen US-002 eval module. If a future story
    wants one source of truth, make resolve canonical and have eval re-export (no cycle:
    eval->resolve->models is clean).
  - value_mapping_store*.py are NOT in the R29 guard core_paths and carry no OMOP
    literals anyway (a row's source_vocabulary/target_field are data the resolver writes).
  - The store is DuckDB-only by design — do NOT add a Databricks mirror; US-010's
    compiler inlines decisions into the staging write instead.
---

## 2026-06-30 - US-006 (Vocabulary resolver — the §4 algorithm)
- Shipped the deterministic OncoTree→SNOMED resolver: distinct source code →
  standard target concept_id (or first-class NO_MAP). Pure-SQL hot path, no LLM.
- NEW src/sema/resolve/candidates.py (step 1a: exact code match in the SOURCE
  vocab via policy.candidate_lookup — R9), standardize.py (step 2: 'Maps to' →
  standard+valid via store.maps_to_targets, relationship/flag from the policy;
  keeps a self-standard candidate; validity-window reduces to invalid_reason IS
  NULL since ConceptRow has no date cols), domain_gate.py (step 3: policy.
  in_target_domain filter), disambiguate.py (step 4: single-model Disambiguator
  protocol + pick_single_survivor default that DECLINES ties — no council).
- NEW src/sema/resolve/engine.py (SCANNED core path — names no domain literal):
  VocabularyResolver thin orchestrator; resolve() composes the 4 stages,
  to_value_mapping/to_assertion/resolve_and_store delegate to engine_utils.
- NEW src/sema/resolve/engine_utils.py (NOT scanned — owns every concept_id
  touch): CodeResolution + ResolveContext dataclasses, classify_resolution
  (1→auto_accepted RESOLVED/Zone-1, 0→NO_MAP/Zone-3, >1→review_pending RESOLVED
  deterministic min-id placeholder/Zone-2), build_value_mapping (§1.5(a) row),
  build_vocab_lookup_assertion (§4 step-5 VOCAB_LOOKUP MappingAssertion; raises
  on NO_MAP). Confidence: 1.0 deterministic auto/NO_MAP, 0.5 ambiguous tie.
- US-006 is the SOLE writer of value-mapping-store rows: resolve_and_store upserts
  one ValueMapping per §1.5(a) grain (incl NO_MAP) into the US-005 store.
- TDD: tests/unit/test_vocab_resolver.py (15 hermetic tests, FakeVocabStore records
  calls) written first — one per stage + RESOLVED/NO_MAP/tie/short-circuit/emit/
  sole-writer/upsert. Failed ModuleNotFoundError, then implemented.
- NEW tests/integration/test_vocab_resolver_live.py (skip-guarded ~/.sema/poc.duckdb,
  reuses tests/integration/_omop_binding.py): LUAD → standard SNOMED 45768916;
  unknown code → NO_MAP; OncoTree→SNOMED path coverage RECORDED at 63/64 (98.4%),
  NO_MAP=1 over the 64 gold codes (threshold deferred to US-012). Ran live: 3 passed.
- Quality: uv run pytest 2008 passed / 1 skipped (+3 live integration); mypy clean
  (192 files); R29 guard clean; coverage 93% repo, 99-100% on the 6 new resolve modules.
- **Learnings for future iterations:**
  - The R29 guard greps resolve/engine.py and `concept_id` is denylisted — see the
    new Codebase Patterns entry. The orchestrator MUST delegate concept-level
    construction to a non-scanned *_utils.py or it trips the guard.
  - ResolveContext carries the per-job refs (source_field_ref, target_property_ref,
    target_field, domain_constraint_ref, vocabulary_ref, vocab_binding, vocab_release,
    resolver_policy_ref, run_id, provenance) as DATA from the caller — US-009 builds it
    from the loaded :VocabularyBinding + source :ValueSet; the engine names none of it.
  - VocabLookup.resolver_policy_ref is a RefStr (needs a dot); OMOP_ONCOTREE_CONDITION_REF
    "omop.oncotree_to_snomed_condition" satisfies the pattern. domain_constraint_ref can use
    '=' (e.g. "target.condition_occurrence.domain=Condition") — the RefStr regex allows it.
  - load_gold_set(path) returns list[GoldRow] (NOT a GoldSet); wrap in GoldSet(rows=...) for
    the labelled/coverage accounting. GoldRow.oncotree_code is the distinct code.
  - The >1-survivor tie path writes a RESOLVED row with status=review_pending (Zone-2), not
    NO_MAP — ambiguity ≠ unmappable. concept_id is a deterministic min-id placeholder so the
    store row is reproducible; the human review (or Slice-1 council) settles it. Slice-0
    OncoTree never hits this (always exactly 1 survivor on the live data: 63/64 resolve, 1
    NO_MAP).
  - US-009 (next) READS resolve_and_store's rows and writes the graph edges; it must NOT
    write the value-mapping store (US-006 is sole writer). to_assertion() gives it the
    VOCAB_LOOKUP MappingAssertion to materialize :FieldMap/MAPS_TO/DERIVED_FROM from.
---

## 2026-06-30 - US-007 (OMOP target via authored manifest + merged loader)
- Authored src/sema/targets/manifests/omop_condition_slice0.yaml: a model_role=TARGET
  OMOP condition_occurrence entity whose condition_concept_id property carries a
  vocabulary_binding {vocabulary: SNOMED (target, R9), domain: Condition,
  require_standard: true, resolver_policy_ref: omop.oncotree_to_snomed_condition (the
  US-004 OMOP/OncoTree policy)}. Plus a minimal omop.person entity as the person_id FK
  target (condition_occurrence required_fields = condition_occurrence_id, person_id,
  condition_concept_id, condition_start_date; NATURAL_KEY PK; FK -> omop.person;
  domain_constraint condition_concept_id -> Condition).
- NO new loader/materializer code: reused load_target (loader.py:36), ManifestTargetAdapter
  (adapters/manifest.py), the materializer -> InMemory/Neo4j writer path, and the existing
  `sema target load --manifest` CLI (cli_target.py:33). The merged framework already emits
  VocabularyBindingOp (with resolver_policy_ref/domain/require_standard) + the
  HAS_VOCABULARY_BINDING RelationshipOp — no framework gap, so no targets/ fix was needed.
- TDD: tests/unit/targets/test_omop_condition_slice0_manifest.py (7 hermetic tests via
  InMemoryGraphWriter: entity/property/obligation/binding ops, HAS_VOCABULARY_BINDING edge
  present + NOT misspelled HAS_VOCAB_BINDING, and normalized binding resolves through
  resolve_policy() to the US-004 ResolverPolicy with source_vocabulary=OncoTree,
  target_domain=Condition). Wrote them first; confirmed FileNotFoundError (manifest absent)
  before authoring the YAML.
- Integration (skip-guarded Neo4j) tests/integration/targets/test_omop_condition_slice0_round_trip.py:
  the AC bridge query (Entity{model_role:TARGET}-[:HAS_PROPERTY]->Property-[:HAS_VOCABULARY_BINDING]->
  VocabularyBinding WHERE property_name='condition_concept_id') returns one row
  (condition_occurrence | Condition | true); HAS_OBLIGATION -> :TargetObligation present.
- Files changed: NEW src/sema/targets/manifests/omop_condition_slice0.yaml, NEW
  tests/unit/targets/test_omop_condition_slice0_manifest.py, NEW
  tests/integration/targets/test_omop_condition_slice0_round_trip.py.
- Quality: pytest 2015 passed / 1 skipped; mypy clean (192 files); R29 guard clean;
  coverage 93%. R29 stays green because the guard only scans *.py — the OMOP literals
  live entirely in the YAML and tests/ (allowlisted).
- **Learnings for future iterations:**
  - :Entity nodes carry `qualified_name`, NOT `name`. The AC's `RETURN e.name` is read in
    the integration test as `e.qualified_name AS name` (and asserted endswith
    'condition_occurrence'). EntityOp has no `name` field at all.
  - :TargetObligation nodes store the whole obligation as a single `payload_json` JSON
    string in Neo4j (target_obligation_merge), NOT as list/scalar properties. Query
    `o.payload_json` and parse/substring-check; `o.required_fields` returns null. (bug-342)
  - The merged targets/ framework ALREADY materializes VocabularyBinding nodes +
    HAS_VOCABULARY_BINDING edges with resolver_policy_ref/domain/require_standard/
    allow_zero_default. US-009 reads this binding (property_name=condition_concept_id) to
    drive the VOCAB_LOOKUP producer — no need to re-emit it.
  - ManifestVocabularyBinding (manifest_models.py:33) already accepts resolver_policy_ref;
    the normalizer rejects unknown vocab refs and dangling FK refs, so a FK requires its
    referenced entity to exist in the manifest (added minimal omop.person).
  - Manifests ship under src/sema/targets/manifests/ (package data; hatchling includes
    non-.py files under the package). CLI takes any path:
    `uv run sema target load --manifest src/sema/targets/manifests/omop_condition_slice0.yaml`.
---

## 2026-06-30 - US-008 (Slice-0 staging obligation + real PlanAssembler)
- Shipped the concrete PlanAssembler that composes MappingAssertions against the
  Slice-0 staging obligation into a MappingPlan with a real verdict — closing the L4
  CONSTANT(NULL) caveat (R19) and replacing the Protocol stub + the hard-coded
  any_review_pending=False at mapping_plan.py:76.
- §1.5(d) status flow: EDIT models/planner/field_map.py — added FieldMap.status: Status
  (default candidate) + covers_required_field() (§1.5(e) gate). EDIT
  models/planner/mapping_plan.py — covered_required_fields() now filters on
  covers_required_field(); derive_verdict() computes any_review_pending from field-map
  statuses; updated PlanAssembler Protocol docstring (points at the concrete impl).
- §1.5(e) coverage rule: a required field is covered ONLY by an accepted, value-producing
  FieldMap. NO_MAP pattern, CONSTANT(NULL) literal, and rejected status never cover.
  condition_concept_id stays REQUIRED (not nullable): per-code NO_MAP is a per-row
  outcome, not an un-covered field.
- NEW src/sema/resolve/assembler.py (R29-SCANNED, generic — names no domain literal):
  Slice0PlanAssembler.assemble groups by target_property_ref, select_winner per group
  (planner mapping_plan.py:112), projects winner→FieldMap with winner.status, emits
  RISK_ASSEMBLER_CONFLICT_RESOLVED (info, non-blocking) on >1 live assertion, derives
  source_scope_ref + lineage from the assertions. Raises on empty/all-rejected input.
- Slice-0 staging obligation in the ALLOWLISTED policies/omop.py
  (make_slice0_staging_obligation): required={condition_concept_id, resolver_policy_ref,
  vocab_release} as dotted RefStr under target.condition_occurrence_staging; NATURAL_KEY
  PK; distinct from the production condition_occurrence obligation (US-007) — no
  person_id, no dates. EDIT planner_loader.py: field_map_to_properties/
  properties_to_field_map round-trip the new status (read .get default candidate).
- TDD: tests/unit/test_plan_assembler.py (16 hermetic tests) written first, failed
  ModuleNotFoundError + missing FieldMap.status/covers/derive behavior, then implemented.
  Covers status default+round-trip, the three non-coverage cases (NO_MAP/CONSTANT-null/
  rejected), awaiting_review on review_pending, the staging obligation shape, PASS/FAIL
  verdicts, status-from-winner, select_winner status-tier, conflict-flag, empty-reject.
- Quality: uv run pytest 2031 passed / 1 skipped; mypy clean (193 files); R29 guard
  clean; coverage 93% repo (planner_loader 100%, omop policy 100%, assembler 97%,
  field_map 98%, mapping_plan 99%). KNOWN pre-existing integration failure bug-347
  (graph/loader test_different_sources_coexist) confirmed failing on HEAD without these
  changes — NOT a US-008 regression; the hermetic unit suite is fully green.
- **Learnings for future iterations:**
  - FieldMap.target_field_ref is a RefStr (regex requires ≥1 dot). Obligation
    required_fields are list[str] (bare allowed) but coverage matching is EXACT string
    equality, so the assembler/producer MUST emit field maps whose target_field_ref is
    byte-identical to the obligation required_fields entries. I used dotted refs for both
    (target.condition_occurrence_staging.<field>); the conceptual field NAMES are the last
    segments. Do NOT switch covered_required_fields to last-segment matching — it would
    break the existing _compilable_plan_factory tests (required uses full dotted ref).
  - derive_plan_verdict checks any_review_pending FIRST → a review_pending FieldMap yields
    awaiting_review regardless of coverage. So coverage and review-state don't conflict.
  - The R29 guard greps resolve/assembler.py and `concept_id` is denylisted. Keep the
    assembler purely generic (it only sees target_property_ref/status as data); the
    OMOP-named staging obligation lives in the allowlisted policies/omop.py.
  - US-009 (next) is the VOCAB_LOOKUP assertion producer; it builds the MappingAssertions
    this assembler consumes (target_property_ref must match the staging obligation's
    required field refs) and writes the :FieldMap/MAPS_TO/DERIVED_FROM graph edges.
  - select_winner (mapping_plan.py:112) is status-tiered then confidence/recency/template;
    a human_pinned assertion beats a higher-confidence auto_accepted one — the assembler
    relies on this, NOT winner_selection.py:24.
---

## 2026-06-30 - US-009 (Deterministic VOCAB_LOOKUP assertion producer + graph edges)
- Built the narrow producer that turns (target binding + source ValueSet + resolved
  decisions) into a column-level MappingAssertion(pattern=VOCAB_LOOKUP) and materializes
  the :FieldMap with MAPS_TO -> target :Property and DERIVED_FROM -> source :Property.
- TDD: wrote tests/unit/test_vocab_lookup_producer.py first (10 hermetic tests, fake
  DecisionSource + fake graph session) — failed on missing module — then implemented.
- Files changed:
  - NEW src/sema/resolve/producer.py — VocabLookupProducer.produce(decisions, policy,
    context, nodes); MappingNodes dataclass; DecisionSource Protocol; reuses
    planner_loader cypher builders for MAPS_TO/DERIVED_FROM + write_field_map.
  - NEW src/sema/resolve/producer_utils.py — decisions_for_binding (scope filter),
    build_column_assertion (fold per-code decisions -> one assertion, status/confidence
    aggregation), field_map_from_assertion, NoResolvedDecisionError.
  - NEW tests/unit/test_vocab_lookup_producer.py (10 tests).
  - NEW tests/integration/test_vocab_lookup_producer_graph.py (skip-guarded Neo4j: AC
    bridge query MATCH (src SOURCE)<-DERIVED_FROM-(fm)-MAPS_TO->(tgt TARGET) -> one row
    ONCOTREE_CODE | VOCAB_LOOKUP | condition_concept_id).
- No MappingProposal; chain is MappingAssertion -> FieldMap -> MappingPlan only. Producer
  NEVER writes the value-mapping store (asserted by test_producer_never_writes...).
- Quality: 2041 unit passed / 1 skipped; mypy clean (195 files); R29 guard clean;
  coverage 93% repo, producer.py + producer_utils.py both 100%.
- **Learnings for future iterations:**
  - VOCAB_LOOKUP is a COLUMN-level mapping (one assertion/FieldMap), per-code decisions
    live in the store and are inlined later by the US-010 compiler — do NOT emit one
    assertion per code.
  - build_vocab_lookup_assertion (engine_utils) only consumes resolution_status,
    source_code, confidence, status from the CodeResolution — concept_id/survivors/chosen
    are unused, so a synthetic column-level CodeResolution reuses it cleanly (DRY: the
    VocabLookup payload is built in exactly one place).
  - planner_loader.cypher_create_field_map_maps_to/derived_from RETURN (stmt, params)
    tuples (they don't execute) — the caller runs session.run(stmt, **params). MAPS_TO
    enforces ModelRole.TARGET, DERIVED_FROM ModelRole.SOURCE.
  - For the Neo4j integration, fetch a target :Property id by querying name+model_role;
    target Property ids are `{model_id}|{version}|{hash}|{entity}.{name}`.
  - planner_loader.py is already 454 lines (>400) — do NOT grow it; reuse its existing
    builders from the caller instead of adding write helpers.
---

## 2026-06-30 - US-010 (Transform compiler: MappingPlan -> SQLGlot -> staging write)
- Built the §1.5(b) transform compiler. `src/sema/compile/compiler_utils.py`
  (R29-scanned, 100% cov): `StagingColumns` (policy-owned physical column names;
  showcase fields have no defaults), `StagingDecision` (generic inlined decision,
  field `target_value` not `concept_id`), `SourceTableSpec`, `CompileContext`,
  `build_staging_select` (one SQLGlot AST: source LEFT JOIN (VALUES ...) ON value=code),
  create/insert/delete/count SQL builders. `src/sema/compile/compiler.py`
  (R29-scanned, 100% cov): `TransformCompiler.compile` dispatches on
  (MappingPattern, TargetArtifactKind) -> `CompiledTransform` (dialect-agnostic AST);
  `.execute` does temp-build + scoped atomic swap on DuckDB. `UnsupportedTransformError`.
- Concept-id projection: `resolve/engine_utils.staging_decision_from_value_mapping`
  (unscanned) reads ValueMapping.concept_id -> StagingDecision so compile/ stays
  R29-clean. OMOP showcase column names in `policies/omop.py:OMOP_STAGING_COLUMNS`.
- Files changed: NEW src/sema/compile/{__init__,compiler,compiler_utils}.py;
  src/sema/resolve/engine_utils.py (+ staging projection helper);
  src/sema/resolve/policies/omop.py (+ OMOP_STAGING_COLUMNS); NEW
  tests/unit/test_transform_compiler.py (12 tests); NEW
  tests/integration/test_transform_compiler_live.py (skip-guarded poc.duckdb).
- Quality: 2053 passed / 1 skipped; mypy clean (198 files); R29 guard clean;
  coverage 93% repo, compiler.py + compiler_utils.py 100%. Live integration on
  poc.duckdb: resolved a real study's distinct ONCOTREE_CODE, executed the staging
  write, row count = source row count, condition_concept_id populated, NULL targets
  subset of NO_MAP codes; Databricks dialect rendered + parsed.
- **Learnings for future iterations:**
  - `concept_id` is a denylist SUBSTRING — never write `.concept_id` (attribute access
    included) in any src/sema/compile/*.py or src/sema/resolve/{engine,assembler}.py.
    Project through a generic shape in an unscanned helper.
  - sqlglot 30: `exp.alias_` returns `exp.Expr` (a Union), NOT `exp.Expression`; mypy
    strict rejects `list[exp.Expression]` of alias_ results — annotate `list[exp.Expr]`.
  - `exp.values(rows, alias=..., columns=[...])` + `.join(values_expr, on=..., join_type=
    "left")` renders `LEFT JOIN (VALUES ...) AS m(...)` (DuckDB) / unparenthesized VALUES
    (Databricks, still parses). Build numeric literals with `exp.Literal.number`, NULLs
    with `exp.null()`.
  - DuckDB idempotent partition swap: `CREATE OR REPLACE TEMP TABLE t AS <select>`, then
    `BEGIN TRANSACTION` / `DELETE ... WHERE scope` / `INSERT ... SELECT FROM t` / `COMMIT`,
    `DROP TABLE IF EXISTS t`. Scope on (source_schema, source_table), not bare schema.
  - Live tests must NOT assert a study contains a NO_MAP code — some studies resolve 100%.
    Assert projection invariants (NULL targets subset of NO_MAP codes) instead.
---

## 2026-06-30 - US-011 (Gate D-lite staging QA)
- Built the staging QA gate (row count, null-rate, NO_MAP accounting vs the gold
  set) so a bad §1.5(b) staging table is caught before it is called "done".
- LOCATION CHOICE: put it in eval/ (not compile/). compile/ is R29-SCANNED and the
  gold-set reconciliation accesses gold.oncotree_code — "oncotree" is a denylist
  substring, so a scanned file would trip the guard. eval/ is allowlisted; the AC
  explicitly permits "compile/staging_qa.py (or eval/)".
- NEW src/sema/eval/staging_qa_utils.py (pure, 100% cov): QAOutcome enum, StagingRow
  (source_value/target_value/resolution_status), QACheck (name/outcome/reason/details
  + passed), StagingQAReport (passed only when ALL checks pass; failures(); as_dict()).
  Three pure checks:
  - check_row_count(actual, expected): staged scope count == source row count.
  - check_null_rate(rows): reconcile NULL <target_concept_column> against
    resolution_status. NO_MAP rows are EXPECTED nulls (not defects); defects are
    resolved_but_null (RESOLVED yet NULL target) and no_map_but_populated.
  - check_no_map_accounting(rows, gold_set): a code the gold set labels RESOLVED but
    staged NO_MAP -> gold_resolved_but_no_map (FAIL, reported not dropped). UNLABELLED
    gold codes are skipped and surfaced in details (no_map_unlabelled_gold).
- NEW src/sema/eval/staging_qa.py (100% cov): read_staging_rows / staging_scope_count /
  run_staging_qa read the staging table via StagingColumns physical names (policy-owned,
  so the module names NO showcase literal) scoped on (source_schema, source_table).
- TDD: tests/unit/test_staging_qa.py (11 hermetic tests) written first, failed
  ModuleNotFoundError, then implemented. Pure-check cases + a temp-DuckDB reader/
  orchestration path (real temp file, in-process = hermetic).
- NEW tests/integration/test_staging_qa_live.py (skip-guarded poc.duckdb): compile +
  execute a real study's US-010 staging write, assert QA PASSES; then NULL out one
  RESOLVED row's target and assert the null_rate check FAILS. Ran live: 1 passed.
- Quality: uv run pytest 2064 passed / 1 skipped; mypy clean (200 files); R29 guard
  clean; coverage 93% repo, staging_qa.py + staging_qa_utils.py both 100%.
- **Learnings for future iterations:**
  - Gold-set reconciliation must live OUTSIDE the R29 core paths (compile/, resolve/
    engine.py, resolve/assembler.py) because GoldRow.oncotree_code attribute access
    contains the "oncotree" denylist substring. Put gold-consuming code in eval/
    (allowlisted) or a non-scanned utils.
  - The §1.5(b) projection invariant (target NULL IFF NO_MAP) is what the null_rate
    check enforces both directions — it is NOT a fixed null-rate threshold; it is
    reconciled per-row against resolution_status. NO_MAP nulls are legitimate.
  - GoldSet.by_code() keys are the raw ONCOTREE_CODE strings, which equal the staging
    source_value_column values — so QA matches staging rows to gold by source value
    directly (no normalization needed for Slice-0 codes).
  - US-012 (next) reuses the SAME gold set + the §1.5(f) metric module
    (eval/mapping_goldset.py) for the confusion-matrix report; it READS the
    value-mapping store, not the staging table. Distinct from this story's QA, which
    reads the compiled staging table.
  - DuckDB UPDATE supports `rowid`; used `MIN(rowid)` to corrupt exactly one RESOLVED
    row in the live test without depending on a specific code.
---

## 2026-06-30 - US-012 (Mapping eval report — confusion matrix vs gold set, thresholds)
- Shipped the end-to-end §1.5(f) report that runs the FROZEN US-002 metric module
  over the resolved value-mapping store (US-005) against the gold set and applies the
  acceptance gate. Re-derives NO math.
- NEW src/sema/eval/mapping_report_utils.py (100% cov): AcceptanceVerdict enum
  (ACCEPTED / PROVISIONAL_NOT_ACCEPTED / RUNNING_NOT_ACCEPTED), thresholds
  MIN_MAPPED_PRECISION=0.95 + MIN_AUTO_RESOLUTION_RATE=0.70, STRUCTURAL_PRECISION_CAVEAT,
  decision_from_value_mapping (bridges resolve→eval ResolutionStatus by value),
  evaluate_acceptance (coverage gate first, then precision+auto thresholds; no_map_accuracy
  reported but NOT gated), and the frozen MappingReport dataclass (as_dict JSON +
  human_summary text; carries score/coverage/verdict/unlabelled_codes).
- NEW src/sema/eval/mapping_report.py (100% cov): decisions_from_store (optional scope
  filters source_vocabulary/target_property_ref/resolver_policy_ref/vocab_release),
  build_mapping_report (score() → evaluate_acceptance), report_from_store convenience.
- TDD: tests/unit/test_mapping_report.py (19 hermetic tests) written first, failed
  ModuleNotFoundError, then implemented. Covers 3-granularity metrics, no_map_accuracy
  reported-separately-not-gated, ACCEPTED at full coverage + thresholds (incl. exact 0.95
  boundary), PROVISIONAL on subset coverage (surfaces unlabelled gap), RUNNING below
  precision / below auto-resolution / undefined-precision, JSON+caveat serialization,
  human_summary None-metric handling, store reads + all scope filters, frozen-ness.
- NEW tests/integration/test_mapping_report_live.py (skip-guarded ~/.sema/poc.duckdb):
  resolves every gold ONCOTREE_CODE via the real §4 resolver into a temp value-mapping
  store, builds the report, prints human_summary; asserts thresholds ONLY at 100%
  human-labelled coverage, else asserts PROVISIONAL + surfaced gap (gold ships UNLABELLED,
  so Ralph never self-certifies — honours the US-002 human gate / US-012 dependency).
- DEPENDENCY: US-012 acceptance CANNOT be self-certified. The gold set is 0/64 labelled
  (UNLABELLED), so coverage=0.0 → PROVISIONAL — not accepted. Full ACCEPTED requires the
  human-label gate (US-002) complete; US-013 acceptance then reads this verdict.
- Quality: uv run pytest 2083 passed / 1 skipped; mypy clean (202 files); R29 guard
  clean; coverage 93% repo, mapping_report.py + mapping_report_utils.py both 100%.
- **Learnings for future iterations:**
  - auto_resolution_rate = zone1/scored and `scored` INCLUDES correctly-predicted NO_MAP
    gold (tn). A gold set heavy with NO_MAP codes lowers auto_resolution even at 100%
    precision — it is NOT tp/RESOLVED-only. Do not "fix" this; it is the frozen US-002
    definition (bug-353). When writing acceptance fixtures, use all-RESOLVED gold to hit
    the ACCEPTED path, or compute zone1/scored by hand.
  - The two ResolutionStatus enums (resolve.value_mapping_store_utils + eval.mapping_
    goldset_utils) are deliberately separate with identical values; bridge via
    `EvalResolutionStatus(store_status.value)`, never import one into the other.
  - eval/ is R29-allowlisted, so reading ValueMapping.concept_id and gold.oncotree_code
    here does NOT trip the guard. Keeping the report in eval/ (not resolve/ or compile/)
    is the same reasoning as US-011's staging QA.
  - US-013 reuses this report at full Databricks coverage; it asserts the thresholds only
    when coverage==1.0 (the live test branches on report.coverage_fraction).
---
